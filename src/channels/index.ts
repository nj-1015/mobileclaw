// Channel self-registration barrel file.
// Each import triggers the channel module's registerChannel() call.

// terminal (MobileClaw: available when no web UI needed)
// import './terminal.js';

// web (MobileClaw: browser-based chat UI on localhost — primary for demo)
import './web.js';

// discord

// gmail
import './gmail.js';

// slack

// telegram

// whatsapp
import './whatsapp.js';
